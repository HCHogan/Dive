import SwiftUI

struct ContentView: View {
    var body: some View {
        Group {
            Text("hehe") 
            Text("hehe")
        }
    }
}

struct CardView: View {
    var body: some View {
        ZStack {
        }
    }
}
